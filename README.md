warrenseymour's zsh configuration
=================================

Usage
-----

Add the following lines to ~/.zshrc:

```
export ZSH_CONF_ROOT=$HOME/Dropbox/zsh
source $ZSH_CONF_ROOT/bootstrap.zsh
```

